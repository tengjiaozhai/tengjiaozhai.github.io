---
title: ✅行程冲突检测工具定义及开发
date: 2026-08-07
desc: 如果不做冲突检测，会发生什么？
category: AI / Agent
tags: ["LLMentor", "工具设计"]
---

# ✅行程冲突检测工具定义及开发

> 来源: https://thoughts.aliyun.com/workspaces/6963289eb0fc2e001bb052eb/docs/6a6a0c92a7c8ff00011d8512

如果不做冲突检测，会发生什么？


* 用户先提交了 2026-08-01 ~ 08-05 上海→北京 出差，一天后 LLM 又收到一句"帮我提 8 月 3 号到 8 月 5 号从深圳出差到广州"——LLM 只看到当前一句请求，**不会** 主动去查历史差旅单来做冲突判断。
* SQL 唯一索引也拦不住："同一员工同一时间段"没有业务字段可以做唯一约束，且日期是**区间** 而不是点。
* 就算写一段"提交前 SELECT 一下"的兜底代码，"重叠但同城"和"重叠且跨城"的处理姿势又完全不同——前者只是提示重复审批浪费，后者是物理不可能。




以上情况都会导致两段行程之间发生冲突，所以本项目把冲突检测提到"工具层"：
1. **让 LLM 显式调用** ：每次提交前必须调 check_travel_order_conflicts，把决策权还给 LLM——它能拿到冲突明细后转述给用户拿确认。
2. **按领域规则分档** ：HIGH/MEDIUM/LOW 三档 + 4 种类型，覆盖"物理不可能 / 衔接紧张 / 同城重复"这三种典型场景。
3. **返回结构化建议 + 一句话摘要** ：suggestion 字段告诉用户"该往哪个方向改"（延后一天 / 调整目的地 / 选早班），summary 字段方便 LLM 在长对话里保留关键信息。




再进一步：为什么不把"是否阻断"的判断也写进 Java 代码？——因为**阻断策略是产品策略** ，会随着运营口径变化，把它写在 prompt 里比写在编译后的类里可运营得多。工具层只输出"事实与建议"，业务策略留给 prompt。


### TravelOrderConflictTools


这个类总共只有一个 @Tool，422 行的类里 200 多行都在拆分冲突场景。先看签名和主流程：



```
@Componentpublic class TravelOrderConflictTools {
/** 视为"同日衔接"允许的最低分钟数（同城默认 0 分钟） */ private static final int SAME_DAY_TIGHT_MINUTES = 8 * 60;
/** 视为"绝对不可能"的天数：同日衔接但所需分钟超过该值 */ private static final int SAME_DAY_IMPOSSIBLE_MINUTES = 24 * 60;
@Autowired private TravelOrderRepository travelOrderRepository; @Autowired private CityTransitTimeService cityTransitTimeService;
@Tool(name = "check_travel_order_conflicts", description = "检查用户当前要提交/修改的差旅单是否与已有生效中差旅单（DRAFT/SUBMITTED/APPROVED）存在冲突。" + "覆盖两类检查：① 同一员工时间重叠（同城=LOW重复，跨城=HIGH物理不可能）；" + "② 跨城市交通衔接合理性（同日衔接时间不足=TRANSIT_TOO_TIGHT；跨城但未衔接=DISCONNECTED_ROUTE）。" + "返回 JSON 结构 {has_conflict, total_conflicts, conflicts:[...], summary}。" + "建议在每次 submit_travel_approval 之前、以及修改差旅单的出发城市/目的地/日期时调用。") public String checkTravelOrderConflicts( AgentSessionContext sessionCtx, @ToolParam(name = "departure_city", description = "候选差旅单的出发城市") String departureCity, @ToolParam(name = "destination", description = "候选差旅单的目的地城市") String destination, @ToolParam(name = "departure_date", description = "候选差旅单的出发日期，YYYY-MM-DD") String departureDate, @ToolParam(name = "return_date", description = "候选差旅单的返回日期，YYYY-MM-DD") String returnDate, @ToolParam(name = "exclude_order_id", description = "需要排除的差旅单ID（修改场景下排除自身），可选", required = false) String excludeOrderId) {
String userId = sessionCtx.getUserId();
// ── 入参校验（错误信息拼成一句话回传 LLM）────────────────────────── List<String> errors = new ArrayList<>(); if (isBlank(userId)) errors.add("user_id 不能为空"); if (isBlank(departureCity)) errors.add("departure_city（出发城市）不能为空"); if (isBlank(destination)) errors.add("destination（目的地）不能为空"); if (!isValidDate(departureDate)) errors.add("departure_date 格式错误或为空，需 YYYY-MM-DD"); if (!isValidDate(returnDate)) errors.add("return_date 格式错误或为空，需 YYYY-MM-DD"); if (!errors.isEmpty()) { return JSON.toJSONString(new ConflictReport() {{ setHasConflict(false); setSummary("入参校验失败：" + String.join("；", errors)); }}); }
LocalDate depDate = LocalDate.parse(departureDate); LocalDate retDate = LocalDate.parse(returnDate); if (depDate.isAfter(retDate)) { return JSON.toJSONString(new ConflictReport() {{ setHasConflict(false); setSummary("出发日期晚于返回日期，请先修正日期。"); }}); }
// ── 拉候选：已有生效中差旅单，且日期区间与候选有交叠 ───────────── Set<TravelOrderStatus> activeStatuses = Set.of( TravelOrderStatus.DRAFT, TravelOrderStatus.SUBMITTED, TravelOrderStatus.APPROVED); List<TravelOrder> candidates = travelOrderRepository.findActiveByUserIdAndDateRange( userId, activeStatuses, departureDate, returnDate);
// 修改场景排除自身 if (!isBlank(excludeOrderId)) { candidates.removeIf(o -> excludeOrderId.equals(o.getOrderId())); }
// ── 逐条判断冲突（一张已有单可能同时触发多类冲突，实际实现每条 return 早退）── List<ConflictItem> conflicts = new ArrayList<>(); for (TravelOrder existing : candidates) { if (!isValidDate(existing.getDepartureDate()) || !isValidDate(existing.getReturnDate())) { logger.warn("跳过数据异常的差旅单 orderId={}", existing.getOrderId()); continue; } detectConflictsFor(existing, departureCity, destination, depDate, retDate, conflicts); }
// 按严重等级降序：HIGH > MEDIUM > LOW，同级按 orderId 稳定排序 conflicts.sort(Comparator.comparingInt((ConflictItem c) -> severityRank(c.getSeverity())) .thenComparing(c -> c.getOrderId() == null ? "" : c.getOrderId()));
ConflictReport report = new ConflictReport(); report.setConflicts(conflicts); report.setTotalConflicts(conflicts.size()); report.setHasConflict(!conflicts.isEmpty()); report.setSummary(buildSummary(conflicts, departureCity, destination, departureDate, returnDate));
return JSON.toJSONString(report); }}
Plain Text

```



**入参校验错误不 throw** ：错误信息拼成一句话塞到 ConflictReport.summary、has_conflict=false 返回。LLM 拿到后能直接把这句话转述给用户，比抛 500 友好一个数量级。


**数据库查询用日期区间交叠** ：findActiveByUserIdAndDateRange 的 SQL 是 WHERE user_id=? AND status IN (...) AND return_date >= departure_date AND departure_date <= return_date——即"已有单的返回日 >= 候选出发日 AND 已有单的出发日 <= 候选返回日"，这是标准的"两个区间是否有交叠"表达式。**注意这一步只圈出"日期相关"的候选，并不代表一定冲突** （毕竟同城重复提交也在这个集合里）；真正定性由后面的 detectConflictsFor 完成。


**返回结构一句话摘要 + 明细列表** ：summary 给 LLM 长对话里保留关键上下文，conflicts 给 LLM 详细展示给用户。两个字段一详一略，都必要。


### 日期冲突检测



```
private void detectConflictsFor(TravelOrder existing, String candidateDepCity, String candidateDest, LocalDate candidateDep, LocalDate candidateRet, List<ConflictItem> out) { LocalDate existingDep = LocalDate.parse(existing.getDepartureDate()); LocalDate existingRet = LocalDate.parse(existing.getReturnDate()); String existingDepCity = existing.getDepartureCity(); String existingDest = existing.getDestination();
// ① 日期完全重叠：[existingDep, existingRet] ∩ [candidateDep, candidateRet] 非空 if (!existingRet.isBefore(candidateDep) && !existingDep.isAfter(candidateRet)) { addOverlapConflict(existing, existingDep, existingRet, existingDepCity, existingDest, candidateDep, candidateRet, candidateDepCity, candidateDest, out); return; // ← 已重叠就不再走衔接类分支 }
// ② 同日衔接（正向）：已有单的返回日 == 候选的出发日 if (existingRet.isEqual(candidateDep)) { addSameDayTransitConflict(existing, existingDepCity, existingDest, candidateDepCity, candidateDest, out); return; }
// ③ 次日衔接（正向）：已有单的返回日 + 1 天 == 候选的出发日 if (existingRet.plusDays(1).isEqual(candidateDep)) { addAdjacentDayConflict(existing, "departure", existingDep, existingRet, existingDepCity, existingDest, candidateDep, candidateRet, candidateDepCity, candidateDest, out); return; }
// ④ 反向同日衔接：已有单的出发日 == 候选的返回日 if (existingDep.isEqual(candidateRet)) { addSameDayTransitConflict(existing, existingDepCity, existingDest, candidateDepCity, candidateDest, out); return; }
// ⑤ 反向次日衔接：已有单的出发日 - 1 天 == 候选的返回日 if (existingDep.minusDays(1).isEqual(candidateRet)) { addAdjacentDayConflict(existing, "return", existingDep, existingRet, existingDepCity, existingDest, candidateDep, candidateRet, candidateDepCity, candidateDest, out); }}
Plain Text

```



这里面具体的addXxxx方法不展开介绍了，大家可以自行看下，其实就针对每种情况做具体的处理的。


### prompt 契约


工具本身只输出"事实"，业务策略在itinerary-manage-agent-system.md。摘录关键几行：

```
1. **发起新差旅前，必须调用 `check_travel_order_conflicts(...)` 检查冲突**。该工具覆盖两类检查： - 同一员工时间重叠（同城=LOW重复提交，跨城=HIGH物理上不可能）； - 跨城市交通衔接合理性（同日衔接时间不足 / 跨城但未衔接）。2. 若用户未提供出发城市，在调用 `check_travel_order_conflicts` 前/后， 调用 `query_user_base_location` 补全 base 地信息。3. 工具返回 `has_conflict=true` 时，按严重等级处理： - **HIGH**（物理上不可能/同日衔接超过 24h）：必须先向用户展示冲突明细并征得用户明确同意后才能提交 （用户回复"确认提交/同意忽略冲突"等明确语义）。 - **MEDIUM**（衔接紧张/路径断裂）：告知用户冲突与建议，让用户选择"调整日期/调整目的地/忽略冲突继续提交"。 - **LOW**（同城市重复提交）：一句话提示即可，不需要额外确认。
Plain Text

```

修改场景对应的 prompt 段

```
3. 当修改会影响日期或城市时（出发日期/返回日期/出发城市/目的地任一变更），必须先调用 `check_travel_order_conflicts` 重新检测冲突，并在用户确认修改前将冲突明细告知用户： - 把所有新字段代入工具调用，并传入 `exclude_order_id` = 当前差旅单ID，避免与自身重复。 - 若存在 HIGH 冲突，必须先让用户明确确认"忽略冲突继续修改"再继续； - 若存在 MEDIUM 冲突，告知用户后由用户决定。
Plain Text

```

这是"工具管事实、prompt 管决策"的完整闭环：Java 里不能做"HIGH 就 throw"这种硬拦截，因为**"是否可以忽略 HIGH"是一个业务决策，未来可能会变** 。写在 prompt 里，产品/运营可以直接调整——不用发版。
